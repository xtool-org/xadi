# xadi

Anisette data generation library used by xtool

## Usage

A self-contained package (pointing at pre-built static libraries) is published to the `release` branch and tagged with `v*`. Supports Linux and macOS.

```swift
.package(url: "https://github.com/xtool-org/xadi", .upToNextMinor(from: ...)),
```
